% postgres, pg

# Import dump to pg database in docker

echo "<dump>"
echo "<container_id">

$ dump: echo "dump"
$ container_id: echo "postgres"

cat <dump_file> | docker exec -i <container_id> psql -U postgres












# print something
echo "My name is <name>!"

$ name: whoami